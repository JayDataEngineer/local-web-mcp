# Caddyfile for MCP Servers
# Tailscale Funnel + Serve handles all HTTPS and path routing.
# Caddy is not used for proxying — Tailscale routes directly.
# This file exists for local development only.

:80 {
    reverse_proxy localhost:8327
}
